# Tab 锁重自学习（V1）

## 使用和存放位置
开启配置项 `整句Tab自学习=是`（默认开启；仅整句模式）。只在 Tab/Shift+Tab 改选后，所选文字真正上屏成功时学习；普通首选、自动提前上屏、取消和未确认的锁定不计数。

```text
<用户数据目录>/码表/<方案名>/.tigirl-learning-v1.log
```

学习文件位于**方案源目录**，不是编译缓存。`.tigirl-learning*` 和 `.tigerclaw-learning*`（不区分 ASCII 大小写）都从码表枚举中排除；即使副本以 `.txt` / `.dict.yaml` 结尾也不会被当成码表。两个产品默认独立保存。

## 学习与排序
按双方候选路径共同的真实编码边界提取纠正片段，最多 16 个 Unicode 字符；前文取当前组合中可靠的最后两个字符。没有可靠前文时使用独立的“未知前文”桶，不假定文档句首，也不读取应用整篇文档。模式、编码规则和方案分别隔离。动态输出、显示/输出别名以及输出转换导致不一致的内容不学习。

相同编码与上下文首次明确纠正加分 6，累计封顶 10，30 天半衰期；近期改选会衰减同组竞争偏好。多字片段至少三次纠正、两个已知上下文，才允许最多 2 分的弱泛化。单字不跨上下文泛化。自动使用首选不会继续强化。

学习参与合法搜索路径的保留；每个位置最多额外保留四条学习路径，前缀检索最多检查 64 个码串行。只使用现有码表允许的边，不创造非法候选、不改变显式选重符号或普通字词模式的码表序号。同一片段的加分只算一次。凡学习影响了搜索集合，该结果不作为概率性或空码提前上屏证据；本次用户主动 Tab 确认仍沿用既有行为。

## 查看、撤销、清空和迁移
工具需要 Python 3.10 或更高版本。`<学习文件>` 必须是上述两个精确日志文件名之一，工具拒绝接受码表文件。路径含空格时加引号。

```console
python tools/learning_records.py "<学习文件>" show
python tools/learning_records.py "<学习文件>" undo
python tools/learning_records.py "<学习文件>" export --file learning-backup.json
python tools/learning_records.py "<学习文件>" clear --yes
python tools/learning_records.py "<学习文件>" import --file learning-backup.json --yes
python tools/learning_records.py "<学习文件>" compact --yes
```

撤销精确移除最后一次纠正事件；一个上屏若包含多个独立纠正片段，需要分别撤销。清空不影响手动加词和码表。导入保持原事件 ID，重复导入不重复计数。导出不会覆盖已有文件。压缩保留已撤销/清空事件的去重标记，避免旧回执复活记录。备份包含个人输入偏好，按个人数据保管；不自动上传或同步。

日志为带 CRC32 的 ASCII 增量格式，文字字段为 UTF-16 十六进制。每个产品采用进程间文件锁，按成功事件串行追加，损坏行跳过、残缺尾行修复。热按键路径只使用内存快照；文件读写在工作线程。最多保留 10,000 个有效事件，写队列上限 256，日志上限 16 MiB。写入失败不影响正常输入；达到上限后先导出，再运行 `compact` 或 `clear`。若连去重标记本身也超过上限，需要停止两个输入法、保留日志备份后另建新日志。文件刷新不是所有应用之间的同步事务：其他进程在资源/解码刷新后看到变化。

关闭开关停止使用学习偏好，但保留文件。旧版虎爪前端没有成功回执能力时不会学习；不能仅更新 Core 而保留旧 TSF 桥并期待本功能生效。

## 验证边界
新增测试使用合成码表、真实会话/解码/存储代码，覆盖取消、重复确认、片段边界、搜索保留、模式隔离、学习置信度隔离、并发写入和维护。它们不等于 Windows 应用中的真实上屏验收。编译、测试的实际结果以交付包里的 `VALIDATION.json` 与日志为准；未运行的检查不得记作通过。

Windows 验收建议：在记事本和浏览器输入框分别 Tab 改选后上屏，确认同样输入的排序改善；取消不增加记录；关闭再开启能恢复偏好；切换方案互不污染；复制 `.log.bak.txt` 后重载码表仍无污染；失败/取消写入不产生学习；启用自动提前上屏后学习不会促成未经独立证据支持的自动提交。
